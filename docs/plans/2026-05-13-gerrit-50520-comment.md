# Gerrit 50520 — Reviewer Ping Comment

This is the exact text intended to be pasted into the REPLY box on
https://review.trustedfirmware.org/c/RF-A/rusted-firmware-a/+/50520
alongside adding reviewers Kis Imre, Andrew Walbran, Mateusz Sulimowicz,
Sandrine Afsa, and J-Ray (for Unsafe-Review).

Companion change: 50500 (arm-firmware-crates/arm-fvp-base-pac), which
this change depends on. Ping 50500 first; that one's CI is already +1.

---

Hi Kis Imre, Andrew, Mateusz, Sandrine, J-Ray,

Gentle ping — this change has been sitting since 2026-04-27 with no
human reviewer attached. The CI red is expected (see below).

Context:

- This is the consumer of Gerrit 50500 (arm-firmware-crates/
  arm-fvp-base-pac, "Add FVP trusted RNG peripheral driver"). It
  replaces NotSupportedTrngPlatformImpl with FvpTrngPlatformImpl,
  adds DEVICE3_RANGE so the peripheral maps MT_DEVICE, and wires it
  in under a SpinMutex during Platform::init().

- TrngPlatformInterface is implemented with REQ_WORDS=2 (two 64-bit
  words per hardware sample cycle, derived from the four 32-bit DATA
  registers) and a stable implementation-defined UUID per DEN0098
  §3.3.

- Patch size: src/platform/fvp.rs +54 / -4. No new unsafe blocks
  introduced beyond the PAC consumption.

- CI is red intentionally — it cannot pass until 50500 lands. That
  dependency is also called out in the commit message body.

I would like to know which of these you prefer:

  (a) a soft review pass now, so this is ready to land the moment
      50500 merges; or
  (b) wait until 50500 is in and revisit then.

J-Ray — flagging for Unsafe-Review because of the SpinMutex-protected
static used to hold the driver instance. Happy to walk through that
bit if useful.

Thanks!
